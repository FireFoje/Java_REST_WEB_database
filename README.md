A site with a database of users and their roles (Admin, User). A user can only visit their own page. Admin can view the list of users, add and remove them. Assign roles to users.

Users:
1. Admin - login: user1, password:user1
2. User - login: user2, password: user2
